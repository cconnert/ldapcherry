admin-super:
    display_name: Administrators Super Level
    description: Administrators of all the systems
    backends_groups:
        ldap:
            - cn=admin-artifactory,ou=groups,o=swarm-analytics,dc=swarm-analytics,dc=com
            - cn=admin-jenkins,ou=groups,o=swarm-analytics,dc=swarm-analytics,dc=com
            - cn=admin-ldap,ou=groups,o=swarm-analytics,dc=swarm-analytics,dc=com

admin-artifactory:
    display_name: Administrators Artifactory
    description:  Administrators of Artifactory
    backends_groups:
        ldap:
            - cn=admin-artifactory,ou=groups,o=swarm-analytics,dc=swarm-analytics,dc=com

admin-jenkins:
    display_name: Administrators Jenkins
    description:  Administrators of Jenkins
    backends_groups:
        ldap:
            - cn=admin-jenkins,ou=groups,o=swarm-analytics,dc=swarm-analytics,dc=com

admin-ldap:
    display_name: Administrators LDAP
    description:  Administrators of the LDAP System
    LC_admins: True
    backends_groups:
        ldap:
            - cn=admin-ldap,ou=groups,o=swarm-analytics,dc=swarm-analytics,dc=com

users-tech:
    display_name: Users Technical
    description: Technical users of the system
    backends_groups:
        ldap:
            - cn=entities-technical,ou=groups,o=swarm-analytics,dc=swarm-analytics,dc=com

users-human:
    display_name: Users Human
    description: Human users of the system
    backends_groups:
        ldap:
            - cn=entities-human,ou=groups,o=swarm-analytics,dc=swarm-analytics,dc=com

users-developers:
    display_name: Users Developers
    description: Software developers within the system
    backends_groups:
        ldap:
            - cn=entities-developers,ou=groups,o=swarm-analytics,dc=swarm-analytics,dc=com
